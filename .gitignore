# Виртуальное окружение и кэш Python
.venv/
__pycache__/
*.pyc

# Данные пользователя — никогда не в репозиторий, даже приватный:
# профили браузера с живыми сессиями аккаунтов (cookies ChatGPT, Perplexity,
# Яндекса, Google — по ним можно войти без пароля), база с результатами и
# зашифрованным ключом OpenRouter, скриншоты, резервные копии, отладочные
# HTML-дампы страниц.
data/

# Браузер Camoufox (~250 МБ), ставится командой `python -m camoufox fetch`
runtime/

# Служебное
.playwright-mcp/
*.log
.idea/
.vscode/
